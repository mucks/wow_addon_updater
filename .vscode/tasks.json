{
    // See https://go.microsoft.com/fwlink/?LinkId=733558 
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargo deploy & run",
            "type": "shell",
            "command": "cd wow_addon_updater_webview && cargo web deploy && cd .. && cargo run",
        }
    ]
}